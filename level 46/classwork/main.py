# List-ს თვისებებია: ordered, can use index, mutable, can be duplicates.
# Tuple-ს თვისებებია: ordered, can use index, immutable, can be duplicates.
# Set-ის თვისებებია: unordered, can't use index, mutable,cannot be duplicates.